import {useState} from 'react';import {useQuery} from '@tanstack/react-query';import {Check,CircleOff,Loader2,RefreshCw,Server,Volume2} from 'lucide-react';import {api} from '../lib/api';import {ApiTestResult} from '../types';
type Tab='AI'|'Voice'|'APIs'|'System'|'Admin';const tabs:Tab[]=['AI','Voice','APIs','System','Admin'];
const Settings=()=>{const [tab,setTab]=useState<Tab>('AI');const [connection,setConnection]=useState<ApiTestResult|null>(null);const [models,setModels]=useState<string[]|null>(null);const [busy,setBusy]=useState(false);const [saving,setSaving]=useState(false);const [saved,setSaved]=useState(false);const [selecting,setSelecting]=useState<string|null>(null);const [selectError,setSelectError]=useState<string|null>(null);const {data,isLoading,error,refetch}=useQuery({queryKey:['settings'],queryFn:()=>api.getSettings()});const testConnection=async()=>{setBusy(true);try{setConnection(await api.testAiConnection())}finally{setBusy(false)}};const loadModels=async()=>{setBusy(true);try{const result=await api.getAiModels();setModels(result.models)}finally{setBusy(false)}};const selectModel=async(model:string)=>{setSelecting(model);setSelectError(null);try{await api.setAiModel(model);await refetch()}catch(err){setSelectError((err as Error).message)}finally{setSelecting(null)}};const [draft,setDraft]=useState<any>();const ai=data?.ai;const current=draft||ai;const update=(key:string,value:any)=>setDraft({...current,[key]:value});const save=async()=>{if(!draft)return;setSaving(true);setSaved(false);try{await api.updateSettings({ai:{provider:draft.provider,base_url:draft.base_url,model:draft.model,timeout_seconds:Number(draft.timeout_seconds),max_tokens:Number(draft.max_tokens),temperature:Number(draft.temperature),top_p:Number(draft.top_p),retry_attempts:Number(draft.retry_attempts),context_size:Number(draft.context_size)}});setDraft(undefined);setSaved(true);await refetch()}finally{setSaving(false)}};if(isLoading)return <div className="flex justify-center py-16"><Loader2 className="w-7 h-7 animate-spin text-accent"/> </div>;if(error||!data)return <div className="card text-red-400">Settings could not be loaded: {(error as Error).message}</div>;const activeModel=current.model;return <div className="max-w-4xl"><div className="flex items-center justify-between mb-8"><div><h1 className="text-3xl font-bold text-gray-100">AI Settings</h1><p className="text-gray-400 mt-2">Provider, model, generation policy, retry behavior and health. Secrets stay encrypted on the backend.</p></div><button className="btn btn-secondary inline-flex gap-2" onClick={()=>refetch()}><RefreshCw className="w-4 h-4"/>Refresh</button></div><div className="flex gap-2 border-b border-surface-border mb-6 overflow-x-auto">{tabs.map(item=><button key={item} onClick={()=>setTab(item)} className={`px-4 py-3 text-sm ${tab===item?'text-accent border-b-2 border-accent':'text-gray-400'}`}>{item}</button>)}</div>{tab==='AI'&&<div className="card space-y-5"><div className="flex items-center gap-3"><Server className="w-5 h-5 text-accent"/><div><h2 className="font-semibold text-gray-100">{current.provider}</h2><p className="text-sm text-gray-400">{current.base_url}</p></div></div><div className="grid grid-cols-2 md:grid-cols-3 gap-4 text-sm">{(['provider','base_url','model','timeout_seconds','max_tokens','temperature','top_p','retry_attempts','context_size'] as const).map(key=><label key={key} className="text-gray-300 capitalize">{key.replace('_',' ')}<input className="input mt-1" value={current[key]??''} onChange={e=>update(key,['timeout_seconds','max_tokens','temperature','top_p','retry_attempts','context_size'].includes(key)?Number(e.target.value):e.target.value}/></label>)}</div><div className="flex gap-3"><button className="btn btn-primary" disabled={saving||!draft} onClick={save}>Save AI settings</button><button className="btn btn-secondary" disabled={busy} onClick={testConnection}>Test connection</button><button className="btn btn-secondary" disabled={busy} onClick={loadModels}>List models</button></div>{saved&&<div className="rounded-lg p-3 text-sm bg-green-900/30 text-green-300">AI settings saved.</div>}{connection&&<div className={`rounded-lg p-3 text-sm ${connection.ok?'bg-green-900/30 text-green-300':'bg-red-900/30 text-red-300'}`}>{connection.ok?`Connected${connection.info?.model?` · ${connection.info.model}`:''}${connection.latency_ms!=null?` · ${connection.latency_ms}ms`:''}`:connection.detail||'Not reachable'}</div>}{selectError&&<div className="rounded-lg p-3 text-sm bg-red-900/30 text-red-300">{selectError}</div>}{models&&<div className="rounded-lg bg-surface-overlay p-4 text-sm"><p className="text-gray-400 mb-2">Available models</p>{models.length?<ul className="space-y-1">{models.map(model=><li key={model}><button type="button" disabled={selecting!==null} onClick={()=>selectModel(model)} className="w-full flex items-center justify-between rounded-md px-3 py-2 text-left hover:bg-surface-border/60 text-gray-200"><span className="truncate">{model}</span>{selecting===model?<Loader2 className="w-4 h-4 animate-spin"/>:model===activeModel?<Check className="w-4 h-4"/>:null}</button></li>)}</ul>:<p>No models were reported.</p>}</div>}</div>}{tab==='Voice'&&<div className="card"><div className="flex gap-3"><Volume2 className="w-5 h-5 text-accent"/><div><h2 className="font-semibold">Voice service boundary</h2><p className="text-gray-400 text-sm mt-1">Whisper endpoint: {data.voice.whisper_url}</p><p className="text-gray-400 text-sm">TTS provider: {data.voice.tts_provider}</p></div></div></div>}{tab==='APIs'&&<div className="card"><h2 className="font-semibold mb-4">Mod catalog sources</h2><div className="space-y-3">{Object.entries(data.mod_sources).map(([source,available])=><div key={source} className="flex justify-between bg-surface-overlay rounded-lg p-3"><span className="capitalize">{source}</span><span className={available?'text-green-400':'text-gray-500'}>{available?'Configured':'Not configured'}</span></div>)}</div></div>}{tab==='System'&&<div className="card"><h2 className="font-semibold mb-3">Safe export pipeline</h2><p className="text-gray-400 text-sm">Exports validate compatible versions, URLs, hashes, paths and loader metadata before replacing an archive.</p></div>}{tab==='Admin'&&<div className="card"><div className="flex gap-3"><CircleOff className="w-5 h-5 text-yellow-400"/><div><h2 className="font-semibold">Admin settings are protected</h2><p className="text-gray-400 text-sm mt-1">Protected configuration is disabled until an admin token is configured.</p></div></div></div>}</div>};export default Settings;
